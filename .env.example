# Path is resolved relative to the website/ root.
DB_PATH=data/boxos.db

# 32+ chars. Generate with: openssl rand -base64 48
SESSION_SECRET=change-me-to-a-long-random-string-please-do-not-ship-default

# First registered username with this name becomes admin (can write blog).
ADMIN_USERNAME=skripsaha
